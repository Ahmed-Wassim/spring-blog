services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: blog
    ports:
      - "5432:5432"
    restart: always

  adminer:
    image: adminer:latest
    ports:
      - "8888:8888"
    restart: always
